[toolchain]
# Nightly is required for the probes crate (bpfel-unknown-none target, build-std).
# The daemon crate compiles fine on stable but we pin nightly for workspace consistency.
channel = "nightly-2025-04-01"
components = ["rust-src", "rustfmt", "clippy"]

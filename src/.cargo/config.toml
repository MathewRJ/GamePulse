[target.x86_64-unknown-linux-gnu]
# Use the system C linker rather than rust-lld. Required for the `ring` crate
# (TLS/crypto library) which compiles assembly via cc and needs the GNU linker
# to resolve its own symbols correctly when built with -C target-cpu=native
# (as set by Arch Linux makepkg).
linker = "cc"
